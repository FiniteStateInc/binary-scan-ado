{
  "name": "binaryscan",
  "version": "0.0.2",
  "description": "The Finite State binary-scan Azure extension allows you to easily integrate the Finite State Platform into your CI/CD pipelines.",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tfx-cli extension create --manifest-globs ../vss-extension.json --rev-version"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "azure-pipelines-task-lib": "^4.4.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
